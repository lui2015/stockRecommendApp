# 「求一票」产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | 求一票 |
| 产品 Slogan | 万一跟你想的一样呢 |
| 文档版本 | V1.1 |
| 创建日期 | 2026-08-04 |
| 最近更新 | 2026-08-05 |
| 文档状态 | 待评审 |
| 适用范围 | H5 /小程序（移动端优先） |

---

## 一、产品背景与定位

「求一票」是一款**娱乐化、游戏化**的选股互动小工具。核心创意来源于"摇奖机 / 老虎机"的交互隐喻：用户摇动屏幕上的摇杆，系统像开奖一样滚动出一个6 位股票代码，并结合 AI（腾讯混元大模型 + 荐股 Skill）给出一只具体的推荐股票和推荐理由，营造"求签、抽奖、碰运气"的轻松氛围。

**产品定位**：
- 这是一个**娱乐/参考类工具**，不是持牌证券投资咨询产品，不构成投资建议。
- 目标用户：对股票感兴趣、喜欢轻互动/彩头玩法的散户用户。
- 核心体验关键词：**趣味性、随机感、仪式感、可分享**。

**合规基调（贯穿全文档，非常重要）**：
- 全站需在关键触点（首页、结果卡片、详情页）展示醒目免责声明："以上内容为AI 生成的数据分析结果，不构成投资建议，股市有风险，投资决策需独立判断"。
- 不出现"必涨""稳赚""目标价必达""内幕""跟庄"等诱导性文案。
- 不收费提供"荐股"服务、不搭售付费咨询，避免触及证券投资咨询业务资质红线。

---

## 二、效果图与页面元素说明

参考效果图（`求一票.png`），页面自上而下由以下模块构成：

1. **顶部导航栏**：返回按钮 + 沉浸式深红背景。
2. **品牌区**：
   - 「求🈁票」Logo（中间以股票K 线图标代替"股"字），风格为红色喜庆底+ 白色描边字体。
   - Slogan：「万一跟你想的一样呢」。
3. **老虎机数字滚轮区**：6 格白底红字数字框（对应 A 股 6 位代码），用户摇动摇杆后数字逐格滚动、依次定格，最终定格出推荐股票代码。
4. **结果卡片区**（票据/彩票样式，带撕边纹理）：
   - 顶部文案「恭喜发财」。
   - 笑脸图标 + 推荐股票名称（如"西部材料"）+ 股票代码（如"002149"）。
   - 卡片背景带半透明水印。
5. **摇杆控件**：卡片右侧一个红色球形摇杆（类似老虎机拉杆/游戏摇杆），是本产品的**核心交互触发器**。
6. **「+ 自选」星标按钮**（P1 新增）：开奖后结果卡片右上角出现圆形星标按钮，默认空心星 ☆，点击加入自选后变为实心金色星 ★ 并带呼吸光效，再次点击取消自选。
7. **底部标签栏**：
   - 「理由」按钮：点击查看本次推荐的详细理由（大师圆桌分析 + 可视化报告）。
   - 「高送转」等**理由摘要标签**：以短标签形式提示本次推荐的核心亮点/题材（如高送转、低估值、AI 概念等），由 AI 推荐结果中的关键标签自动生成，随每次推荐结果变化。

---

## 三、核心用户流程

```
进入首页
  → 用户摇动/拖拽摇杆（或点击"摇一次"按钮）
    → 触发老虎机数字滚动动画（前端表现，2~3秒）
    → 前端并发调用后端「求一票」接口
      → 后端组织Prompt，调用【荐股 Skill】完成选股与理由生成
      → 荐股 Skill 内部按需调用【腾讯混元大模型】做语义生成/归纳
    → 后端返回：股票代码、名称、核心标签、精简理由、详情报告
  → 数字滚轮定格 = 返回的股票代码，结果卡片浮现动画展示
  → 用户可点击「理由」查看详情（大师圆桌完整理由 + 风险提示 + 可视化报告）
  → 用户可点击「保存」将本次结果存入"我的求票记录"
  → 用户可重复摇动，生成新的一次推荐（限流保护）
```

---

## 四、核心功能需求

### 4.1 摇杆交互（P0）

- **交互方式**：
  - 桌面/H5 环境：鼠标按住摇杆向任意方向拖动一定距离后释放，触发一次"求票"。
  - 移动端：支持触摸拖动摇杆；若浏览器/设备支持 `DeviceMotionEvent`，同时支持"真实摇一摇手机"触发（需用户授权动作传感器权限，iOS 13+ 需显式请求权限）。
  - 兼容降级：无摇杆/传感器权限时，摇杆区域本身可直接点击触发。
- **防误触/防连点**：单次求票动画完成前禁止重复触发；同一用户短时间内多次求票需前端节流（如3 秒内只生效一次）。
- **动效要求**：摇杆按压回弹动效、数字滚轮逐位定格动效（类似老虎机开奖），营造仪式感。

### 4.2 AI 荐股结果生成（P0）

- 每次"求票"由后端统一编排：
  1. 接收用户请求（可选携带简单偏好参数，如市场/板块/风格，见 4.5）。
  2. 调用【荐股 Skill】能力，获得：
     - 推荐股票代码 + 名称（唯一，不给并列选项）
     - 大师圆桌推荐理由（六位投资大师视角）
     - 至少2 条反面风险提示
     - 核心标签（如"高送转""低估值""AI 概念"等，用于结果页标签展示）
     - 可视化 HTML 深度报告（详情页展示用）
  3. 若荐股 Skill 需要语言组织/归纳（如生成"一句话理由"摘要、生成结果卡文案），通过腾讯混元大模型接口完成文本生成。
- **数据真实性要求**：结果中的行情/财务数据必须来自荐股 Skill 依赖的真实数据源（westock-tool/westock-data），大模型仅负责语言表达，不得凭"记忆"编造数据（沿用 Skill 自身设计原则）。

### 4.3 结果展示（P0）

- 数字滚轮最终定格展示股票代码（逐位滚动动画）。
- 结果卡片展示：
  - 祝福语（如"恭喜发财"，可从文案库随机/根据结果情绪选取）
  - 股票名称 + 代码
  - 1~2 个核心标签（底部标签栏，如"高送转"）
- 免责声明常驻展示于卡片下方（小字号，不可关闭/隐藏）。

### 4.4 查看详情（P0）

- 点击「理由」标签，进入详情页/弹层，展示：
  - 大师圆桌完整推荐理由（六位大师各自的判断依据，含引用的量化数据）
  - 至少 2 条风险提示（反面意见）
  - 可视化 HTML 深度报告（iframe 或转图嵌入展示）
  - 数据来源与更新时间
  - 醒目免责声明（同4.3，样式更完整）

### 4.5 保存本次推荐结果（P0）

- 结果卡片/详情页提供「保存」按钮，将本次求票结果存入用户的「求票记录」列表，包含：
  - 求票时间
  - 股票代码/名称
  - 核心标签
  - 理由摘要（详情可点击展开）
  - 当时的约束条件（若有）
- 「求票记录」列表页：按时间倒序展示历史保存记录，支持删除单条记录、支持点击回看详情。
- 未登录用户：可本地存储（localStorage/浏览器存储）保留最近 N 条；若产品接入用户账号体系，则登录后持久化到服务端并支持多端同步（本版本可作为 P1 增强项，视是否有账号体系而定）。

### 4.6 约束条件输入（P1，可选增强）

- 参考荐股 Skill 支持的约束类型，页面可提供简单的"许愿条件"选择（可选，非必填）：
  - 市场：A股/港股/美股
  - 板块：白酒/半导体/新能源/AI 等（预置常见板块）
  - 风格：价值/成长/高股息
- 用户选择后再摇摇杆，约束条件随请求传给后端，影响荐股 Skill 的候选池筛选。

### 4.7 分享（P2，后续迭代）

- 结果卡片可生成分享图（含股票代码、标签、Slogan），支持保存图片/分享至社交渠道。
- 分享文案需保留免责声明，不得二次传播时丢失合规提示。

### 4.8 我的自选（P1，新增）

- **加入自选**：开奖后结果卡片右上角出现「+ 自选」星标按钮，点击将当前推荐股票加入自选列表，按钮变为金色实心星 ★（带呼吸光效）；再次点击取消自选。
- **自选入口**：首页右侧抽屉导航（与「我的记录」并列）新增「⭐ 我的自选」入口，进入自选列表页。
- **自选列表页**（`my-favorites.html`）：
  - 三态展示：加载中（spinner）、空状态（引导去摇一摇）、列表态。
  - 每只自选股展示：股票名称、6 位代码、市场标签、加入时间、操作按钮（深度分析 / 移除）。
  - **价格展示（关键）**：
    - **推荐时价格**：加入自选时刻的股票价格（`favorites.price`，摇号时由后端推荐结果中的 `price` 字段带入）。
    - **当前实时价格**：列表加载时实时调用行情接口（`quoteService.fetchLatestQuote`）获取，与推荐时价格并列展示，并计算**涨跌幅**与**盈亏差价**。
    - 涨跌以红涨绿跌配色（通用 A 股习惯：红=涨、绿=跌），展示形式：`[推荐价] → [实时价] (当日涨跌幅%)  盈亏差价 (百分比%)`。
  - 点击「深度分析」跳转深度报告页（`analysis.html?code=xxx&name=xxx`）。
  - 点击「移除」二次确认后删除（接口级归属校验）。
- **数据隔离**：自选列表按匿名设备标识（X-Device-Id）隔离，不同设备互不干扰；无账号体系时依赖设备指纹。
- **设计约束**：星标按钮需融入卡片视觉（圆形、半透明底、金色系），不可用突兀的橙色药片样式。

---

## 五、接口与技术方案

### 5.1 整体架构

```
前端（H5/小程序）
   │1. 用户摇杆触发
   ▼
后端服务（Node.js/Express 等）
   │  2. 组织求票请求
   ▼
荐股 Skill（stock-recommend-skill）
   │  3a. 调用 westock-tool / westock-data 获取真实行情与财务数据
   │  3b. 需要语言生成/归纳时调用——
   ▼
腾讯混元大模型（hy3, tokenhub.tencentmaas.com）
   │  4. 返回生成文本
   ▼
后端汇总结果 → 落库（可选）→ 返回前端
```

### 5.2 荐股 Skill 集成

- 安装方式（部署环境执行，需人工确认后操作）：
  ```bash
  git clone https://github.com/lui2015/STOCK-RECOMMEND-SKILL.git \
    ~/.workbuddy/skills/stock-recommend-skill
  ```
- 调用方式：后端以"推荐一只股票"或"推荐一只 {板块} 股票"等自然语言指令触发 Skill 执行链路，获取结构化结果（代码/名称/理由/标签/报告）。
- 依赖组件 `westock-tool`、`westock-data`、`stock-analysis` 均为平台内置 Skill，需确认部署环境已具备调用能力。
- 已知限制（需在实现时规避，详见 Skill README"踩坑记录"）：
  - 板块筛选需用 `filter --universe`，不可用 `ranking --universe`；
  - 港美股低PE 筛选需设PE 下限，避免筛出异常股；
  - A股与港美股字段名不同，不可混用；
  - 多条件组合需用 `intersect([...])`。

### 5.3 腾讯混元大模型接口

- Endpoint：`POST https://tokenhub.tencentmaas.com/v1/chat/completions`
- 请求示例：
  ```bash
  curl -X POST 'https://tokenhub.tencentmaas.com/v1/chat/completions' \
    -H 'Authorization: Bearer ${HUNYUAN_API_KEY}' \
    -H 'Content-Type: application/json' \
    -d '{
      "model": "hy3",
      "messages": [
        {"role": "system", "content": "..."},
        {"role": "user", "content": "..."}
      ],
      "stream": false
    }'
  ```
- **安全要求（强制）**：
  - `HUNYUAN_API_KEY` 必须存放于服务端环境变量（如 `.env`，不入库、不硬编码、不暴露给前端/客户端代码），严禁将本文档中出现的示例 Key 直接用于生产环境，生产环境需重新申请/轮换密钥。
  - 该接口只能由**后端**代理调用，前端不得直接持有/请求该接口，避免密钥泄露与被恶意刷量。
  - 后端需对该出站请求做超时控制（建议 10~15s）与失败重试/降级（如失败时返回预置的"求票失败，请重试"提示，而非报错堆栈）。
  - 生产环境如需限制出站访问范围，仅允许访问 `tokenhub.tencentmaas.com` 等已知域名，禁止透传用户可控 URL，防止 SSRF 风险。

### 5.4 后端接口设计（本产品自身API）

#### `POST /api/draw`（求一票）

请求：
```json
{
  "constraints": {
    "market": "A股",      // 可选：A股/港股/美股
    "sector": "白酒",      // 可选
    "style": "成长"        // 可选：价值/成长/高股息 等
  }
}
```

响应：
```json
{
  "requestId": "uuid",
  "code": "002149",
  "name": "西部材料",
  "tags": ["高送转"],
  "summaryReason": "一句话摘要理由（用于结果卡展示）",
  "reasonDetail": {
    "roundtable": [
      { "master": "巴菲特", "viewpoint": "……护城河分析……" },
      { "master": "段永平", "viewpoint": "……生意模式分析……" }
    ],
    "risks": ["风险提示1", "风险提示2"]
  },
  "reportUrl": "/reports/{requestId}.html",
  "dataAsOf": "2026-08-04",
  "disclaimer": "以上内容为AI生成的数据分析结果，不构成投资建议，股市有风险，投资决策需独立判断。"
}
```

#### `POST /api/history`（保存本次结果）

请求：携带 `requestId`（关联 `/api/draw` 返回的结果，服务端按 requestId 查已生成内容存档，避免前端伪造/篡改结果内容）。

响应：`{ "success": true, "historyId": "..." }`

#### `GET /api/history`（获取求票记录列表）

- 支持分页；仅返回当前用户（或匿名设备标识）自己的记录。

#### `DELETE /api/history/{id}`（删除单条记录）

- 需校验记录归属，防止越权删除他人记录（AuthZ）。

#### `POST /api/favorites`（加入自选）

请求体：
```json
{
  "code": "002149",
  "name": "西部材料",
  "market": "A股",
  "price": 12.34      // 可选：加入自选时的推荐价，用于后续盈亏对比
}
```

响应：`{ "success": true, "existed": false }`（`existed=true` 表示此前已自选，重复添加不报错）

#### `GET /api/favorites`（获取自选列表，含实时行情）

- 按当前设备标识返回自选列表。
- 列表每条**并发调用行情接口获取实时价**，返回字段含 `refPrice`（推荐时价）、`realtimePrice`（实时价）、`changePercent`（当日涨跌幅 %）。
- 行情接口异常时降级返回无实时价的列表，不阻断页面。

响应示例：
```json
{
  "items": [
    {
      "id": 1,
      "code": "002149",
      "name": "西部材料",
      "market": "A股",
      "refPrice": 12.34,
      "realtimePrice": 12.80,
      "changePercent": 3.73,
      "addedAt": "2026-08-05T10:00:00.000Z"
    }
  ]
}
```

#### `DELETE /api/favorites/{code}`（移除自选）

- 需校验设备归属，防止越权删除他人自选。

#### `GET /api/favorites/check/{code}`（检查是否已自选）

- 返回 `{ "isFavorite": true/false }`，供首页结果卡片初始化星标状态。

### 5.5 数据存储

- `draws` 表：记录每次生成结果（requestId、原始约束、股票代码/名称/标签、理由、报告路径、生成时间），用于 `/api/history` 保存时按ID 落档，避免明文传输被篡改。
- `history` 表：用户/设备维度的"已保存"记录，关联 `draws.requestId`。
- `favorites` 表：用户/设备维度的自选股列表，字段含 `device_id`、`code`、`name`、`market`、`price`（推荐时价，可空）、`added_at`，唯一约束 `(device_id, code)`，索引 `idx_favorites_device`。
- 若无账号体系，可用匿名设备指纹/Cookie 区分归属；后续接入登录体系后再做数据迁移绑定。

---

## 六、非功能性需求

### 6.1 性能

- 单次"求票"端到端响应（含前端动画）控制在 5秒内完成为佳；若大模型/Skill 调用耗时较长，前端动画时长应与实际请求做异步解耦（动画可先行播放，等待结果返回后再定格）。
- 后端对荐股 Skill / 大模型调用增加超时与降级策略，避免长时间挂起。

### 6.2 限流与防刷（安全 + 成本控制）

- 大模型调用与荐股 Skill 调用均产生成本，需对单用户/单IP 做频率限制（如每分钟最多 N 次、每日最多 M 次），防止恶意刷量消耗资源。
- `/api/draw` 接口需做基础的接口鉴权/来源校验（如同源校验、简单的请求签名或验证码兜底），避免被脚本批量刷取导致 API Key 额度耗尽。

### 6.3 安全

- 遵循 Secrets 环境变量管理：`HUNYUAN_API_KEY` 等所有密钥仅存于服务端环境变量，不写入代码库、不下发到前端。
- 所有数据库查询使用参数化/ORM 绑定，禁止字符串拼接 SQL（防 SQLi）。
- `/api/history` 系列接口需做归属校验（AuthZ），用户只能查看/删除自己的记录。
- 出站请求（大模型、Skill 内部数据源）需限定域名白名单，不接受用户输入拼接目标地址，防止 SSRF。
- 前端展示大模型/Skill 返回的理由文本时需做转义/富文本净化（防XSS），尤其是可视化 HTML 报告的嵌入展示需使用受限iframe（sandbox 属性）或安全的 HTML 净化库处理后再渲染。

### 6.4 合规

- 全局免责声明常驻展示（首页、结果卡、详情页均需出现），不可被用户关闭。
- 文案审核：结果摘要、理由生成文本中不得出现"稳赚""必涨""内幕""跟庄"等诱导性/承诺性用语，可在后端对大模型输出做敏感词过滤兜底。
- 若产品后续考虑商业化（会员/打赏/广告），需评估是否涉及证券投资咨询资质要求，谨慎处理，避免触碰"以营利为目的提供股票推荐"的监管红线。

### 6.5 兼容性

- 移动端浏览器（iOS Safari / 微信内置浏览器 / Android Chrome）适配为主。
- 无摇一摇传感器权限或桌面端场景下，摇杆点击/拖动作为主交互降级方案。

---

## 七、异常与边界场景

| 场景 | 处理方式 |
|---|---|
| 荐股 Skill 调用超时/失败 | 前端提示"求票失败，请重新摇一摇"，不展示假数据 |
| 混元大模型接口异常（限流/超时） | 后端降级为使用 Skill 原始结构化文案，不强依赖大模型生成措辞 |
| 短时间内重复摇杆 | 前端节流 + 后端限流双重拦截，提示"手速太快啦，稍等一下" |
| 用户未授权设备摇一摇权限 | 静默降级为拖拽/点击摇杆交互，不强制弹权限申请 |
| 保存记录已达上限（如匿名用户本地存储条数上限） | 提示"记录已达上限，删除旧记录后可继续保存" |
| 越权访问/删除他人历史记录 | 接口层校验拒绝，返回 403 |

---

## 八、验收标准（Acceptance Criteria）

1. 用户可通过拖动/点击摇杆（或真实摇动手机，视权限）触发一次"求票"，2~3 秒内看到数字滚轮开奖动效并定格出结果。
2. 结果卡片正确展示推荐股票名称、代码、核心标签，且免责声明始终可见。
3. 点击「理由」可查看完整大师圆桌理由、风险提示与可视化报告。
4. 点击「保存」后，该结果出现在"求票记录"列表中，可回看、可删除，且不可查看/删除他人记录。
5. 连续快速触发摇杆时，系统有节流保护，不会产生异常并发请求。
6. 压测/审查确认：大模型 API Key 仅存在于服务端环境变量，前端代码及网络请求中不可见明文密钥。
7. 恶意/异常输入（如约束参数篡改、非法 requestId）不会导致服务端报错泄露堆栈信息或越权数据访问。
8. 开奖后结果卡片「+ 自选」星标按钮可加入/取消自选；「我的自选」列表正确展示推荐时价与当前实时价及涨跌幅，并可移除与跳转深度分析。
9. 自选数据按设备隔离，越权访问/删除他人自选被接口层拒绝。

---

## 九、后续迭代规划（不在本期范围）

- 分享结果图到社交平台。
- 账号体系与多端同步的"求票记录"与"我的自选"。
- "许愿条件"更丰富的自定义（自选大师理念偏好等）。
- 自选股价格变动推送 / 涨跌幅提醒。
- 每日求票次数限制 + 会员权益体系（需先完成合规评估）。
